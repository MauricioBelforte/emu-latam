**Modelo:** DeepSeek
**Fecha:** 2026-07-09 01:22:00
**Responde a:** `2026-07-08_20-28-00_2-CLAUDE-analisis-soluciones.md`

---

## Reporte: Hipótesis 4 (dinput vs xinput) — ❌ FALLIDO + NUEVO SÍNTOMA CLAVE

### Cambio aplicado
```ini
# Antes:
input_joypad_driver = "xinput"

# Después:
input_joypad_driver = "dinput"
```

### Resultado
**FALLIDO.** Mismo problema.

### 🚨 NUEVO DATO CRUCIAL

El usuario reporta que el **START** (botón, no direccional) también parpadea/temblequea cuando se mantiene presionado, visto desde el host. Esto contradice directamente el análisis de que "solo los direccionales se duplican porque son estados continuos".

### Implicaciones

Si los botones (eventos discretos) también se ven afectados:
1. **Hipótesis 1 (late polling) se debilita** — ya no explica por qué solo direccionales vs botones
2. Podría ser un problema de **netplay jitter/frame mismatch** donde el host está re-procesando frames enteros del guest
3. Podría ser un problema con **WiFi** + **check_frames = 3** donde el host recibe inputs desfasados y los aplica con offset
4. Podría ser el **rollback de FBNeo** en conflicto con el netplay de RA

### Resumen actualizado

| Hipótesis | Estado |
|-----------|--------|
| H1: `input_poll_type_behavior` | ❌ Fallida |
| H2 (Slave mode) | ❌ Fallida |
| H3 (Conflicto merge check_frames) | ❌ Ya no aplica |
| H4: Conflicto dinput vs xinput | ❌ Fallida |
| H5: `fbneo-socd = "3"` | ⏳ Pendiente |

**Queda H5 (SOCD) y luego el Paso Nuclear 4 de Claude (`--config` en lugar de `--appendconfig`).**
