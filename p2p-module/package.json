{
  "name": "emu-latam-p2p",
  "version": "1.0.0",
  "description": "Módulo P2P independiente para Emu Latam — NAT hole punching, relay, keepalive",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "node --loader ts-node/esm examples/integration-test.ts",
    "build": "tsc",
    "example": "node --loader ts-node/esm examples/simple-p2p.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0",
    "ts-node": "^10.9.0"
  },
  "dependencies": {}
}
